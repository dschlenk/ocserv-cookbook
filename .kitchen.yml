---
driver:
  name: vagrant
  customize:
    natdnshostresolver1: "on"

provisioner:
  name: chef_zero

platforms:
  - name: centos-7.2
  - name: centos-6.8

suites:
  - name: default
    run_list:
      - recipe[ocserv::default]
    attributes:
